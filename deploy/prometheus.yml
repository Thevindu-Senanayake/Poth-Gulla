global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'poth-gulla-api'
    # In the deploy stack the API runs as the in-network service `backend`.
    static_configs:
      - targets: ['backend:3000']
    metrics_path: '/api/metrics'
